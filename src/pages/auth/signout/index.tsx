import { Box, Button, Typography } from "@mui/material";

const SignOutPage = () => {
  return (
    <Box>
      <Button variant="contained">Sign out</Button>
      <Typography>Custom signout page.</Typography>
    </Box>
  );
};

export default SignOutPage;
